# 安装与首次验收

将完整 `data-to-value` 文件夹或ZIP导入支持技能的宿主，保留 `references` 和 `scripts`。不能只上传 `SKILL.md`。

## 首次导入后

若宿主支持安装初始化，让它执行下面的维护任务；若导入器仅保存文件，可在导入后发送一次：

> 请为刚导入的 data-to-value 完成一次安装初始化。读取 references/runtime.md，在技能目录下执行 scripts/setup_environment.py --profile common --install（工作目录不是技能目录时改用绝对路径；宿主受限导致默认状态目录不可写时，加 --state-dir 指向授权范围内的可写目录），等待完成并检查逐格式读取样例及依赖版本结果。记录 runtime-common.json 中的解释器路径，后续复用，不要反复安装。此次仅做安装维护，不选择学术版或企业版，不读取我的项目数据。另检查偏好页和评分页是否能在当前宿主展示；无法自动回传时如实说明。请区分已导入、common环境已就绪、页面交互已验证。

具体命令、持久目录和失败处理见 [运行环境](references/runtime.md)。安装程序仅在缺少依赖时创建技能专用虚拟环境；现有环境满足要求则复用。无法运行Python的平台仍可遵循文本流程，但不算完整脚本能力已安装。依赖约束文件并不会让所有宿主自动安装。

## 后续使用

新建任务，提供数据及说明，发送“请使用 data-to-value 发现有价值的想法”。没有明确选择时，技能应在第一步同时确认学术版/企业版与权重；确认后再审核数据并生成候选。已有版本或偏好直接沿用。日常使用不需再次初始化，除非运行环境丢失或改变。

在网页版宿主里，偏好页与比较页生成到会话工作区后由平台的交付/预览入口打开；页面回传不受保证（沙箱iframe无法访问本机回环地址、剪贴板也可能不可用），因此**偏好确认与方案选择以对话中的明确回复为准**，页面用于查看和调权，不作为确认渠道。

发布包不包含用户数据、虚拟环境和本机验收回执，也不包含 `__pycache__`、`runtime-common.json` 等运行产物；打包前删除这些内容。当前宿主的实际测试结果不能从其他电脑的成功记录推断。

## 可选OCR

安装时已知需要图片/扫描件的用户，选择 `--profile ocr --install --language eng+chi_sim`。除Python包外，需要管理员或平台在安装阶段准备Tesseract可执行文件和指定语言数据；脚本不会静默安装系统程序或下载语言包。缺少引擎/语言时验收失败，不能承诺图片已支持。后续按 `runtime-ocr.json` 使用已验收环境，读取时显式加 `--ocr --language eng+chi_sim`。

格式与平台实测范围见 [兼容性矩阵](references/compatibility.md)。首次安装验收完成后，日常数据读取不会自行安装依赖。
