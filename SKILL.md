---
name: data-to-value
metadata:
  version: "0.1.0"
description: "从实际数据和项目资料出发，提出、审查并落实有价值的想法，形成有证据的学术文稿或企业应用方案。启动时请使用者选择学术版或企业版。用于数据驱动选题、研究设计、方案审计、实验到论文写作，以及从企业尚未充分利用的数据中发现并开发业务机会；单纯文字润色或已明确规则的文件转换不需启动完整流程。"
---

# 数据驱动研究与价值落地

使重要判断能沿着这条链追溯：**实际数据 → 可回答的问题 → 学术或业务价值 → 可证伪的想法 → 低成本验证 → 工程实现 → 有证据的表达。** 数据驱动不是只寻找相关性；用领域知识、现有研究和真实决策约束问题，防止从大量尝试中挑一个好看的结果。

学术研究和企业应用地位相同，由使用者选择对应流程。学术版从数据发现值得研究的问题；企业版从尚未充分利用的数据发现新的业务机会，形成可增加收益、降低成本或改善决策的想法。企业版不要求使用者预先提供业务方案或收益假设；价值检验发生在机会提出之后。不要把企业收益等同论文创新，也不要把算法准确率直接等同业务收益。

## 启动时先询问使用者

保留一个技能入口，提供两个版本。**每个新任务首次启动时，先询问使用者选择学术版还是企业版，收到明确选择后再开始对应工作。** 不根据“论文”“收益”等措辞自行判定；若使用者已经明确说“选择学术版”或“选择企业版”，视为已回答，不重复询问。

使用环境提供的输入工具（若可用）提出下面的问题；否则用简短文字。两项同等展示，不标记某项为推荐或默认：

> 这次希望使用哪个版本？
> - 学术版：从数据发现研究问题，形成可验证的方案和有证据的论文。
> - 企业版：从尚未充分利用的数据发现业务机会，形成可实施的增收、降本或决策改进方案。

**若本任务没有用户明确选择版本的原话或已确认记录，首次回复只提出上述版本选择问题，等待回答。** 不因文件像企业数据、材料提到论文、历史演示使用某模式，或其他工具建议而代选。版本确认前不检查主机、安装依赖、读取数据、检索数据集或开展选题；未回复不等于默认选择。使用者主动要求兼顾两者时，明确主交付与附加目标，共用证据但分别评价，不自行增加第三个默认入口。

安装初始化是单独的维护任务，不替用户选择项目版本。执行安装、依赖准备或环境恢复时读 [安装与运行环境](references/runtime.md)；默认一次准备common常用数据配置，OCR单独选配。正常调用使用回执里的解释器及 [统一读取入口](references/data-formats.md)，不在上传后临时pip安装。遇到未准备能力、未知格式或损坏文件，记录具体状态和影响；不得标为已审核。偏好页和评分页只依赖Python标准库，不需要pandas或openpyxl。

| 版本 | 工作起点 | 主要交付与专用细则 |
|---|---|---|
| 学术版 | 数据可支持的现象、问题与研究空白 | 可检验问题、贡献边界、实验、证据到文稿；读 [学术细则](references/academic-writing.md) |
| 企业版 | 企业现有数据及其尚未开发的用途；可以没有预定业务方案 | 数据用途与机会清单、候选想法、优先方案、最小试点与实施交接；读 [企业细则](references/business-value.md) |

同一任务记录使用者已选版本，续作时沿用。使用者明确切换时更新选择，复用证据，不重启所有读取；不要将学术与企业的成功标准相互替代。这项版本选择不改变下文“改变已确认的目标或主线前先讨论”的协作约定。

## 进入任务

- 版本选定后，读取当前授权范围内的材料和既有决定，区分本轮交付是机会发现、选题、审计、实验、论文还是业务试点。用户只要其中一环时，完成这一环所需的证据检查，不强制重做整套项目。
- 新项目：列清数据、资料、行业/领域背景、使用者及约束。没有预定问题是允许的：先核验数据能观察什么、目前已用于什么，再据此提出可讨论的研究问题或业务机会。只询问会改变方向的少量背景问题，不要求使用者先提出想法；已有业务目标或研究问题应被检验和完善，不自动替换。
- 延续项目：核对关键文件版本、路径和证据范围后复用已完成审计；不要因换模型、换目录或换任务名称把旧开发集称为新盲测。
- 用户已明确无法补充的信息记为未知，不反复索取。没有任何可读取数据时，只交付条件性框架和所需最小材料，不能提出声称已由数据支持的结论。
- **在已授权的机会发现范围内，可淘汰不成立的候选并继续寻找其他候选，记录原因，无需逐项询问。** 需要改变用户已确认的主要目标、已选主线、交付类型，或显著扩大投入与授权范围时，先讨论再推进。说明障碍、可保留的目标与调整代价；可以提出备选，但用户回应前不将其设为新主线或启动其开发。等待期间可继续与选择无关的核验。同目标下的字段修正、评价器修复和已授权实现可以继续；已有明确授权的替代方向不重复确认。
- 技能不扩大任务授权：研究方案不意味着自动运行昂贵实验、采购、公开私有材料或改变生产流程。停止条件针对不成立的路径，不意味着放弃用户的总体目标。

依据用户本轮请求选择入口和交付深度；下文各阶段按需使用，不要求依次全部执行：

| 本轮请求 | 主要出口 |
|---|---|
| 机会发现／选题 | 有依据且实质不同的候选；默认精选三个作评分与可视化比较，展示依据、成熟度及关键未知，等待用户选择 |
| 数据核验／方案审查 | 相关事实、问题及影响、修正建议与验收标准 |
| 最小验证 | 已授权验证的实际结果，以及继续、修正、收缩或停止的判断 |
| 实施／写作 | 与请求匹配的可执行交付，或与现有证据一致的文稿 |

仅做选题时不自动训练模型、编排完整工程任务或撰写论文；仅做写作时复用已有结果并补查影响主张的证据。涉及多方案选择时，最终由用户选定主线；一般性的“完整推进”不等于把选方案交给技能。用户已明确选定方案且要求完整推进时，在授权范围内衔接各阶段，无需逐阶段重复确认。

## 想法生成前先确认偏好

本轮需要发现并比较多个想法时，选定版本、明确本轮范围后，**先按 [候选评分与用户选择](references/candidate-scoring.md) 展示偏好设置页，让用户调整各项指标权重或明确接受初始权重，再审核数据并生成候选。** 不把完整数据审核、表格依赖安装或外部表格技能的通用流程当作展示偏好页的前置条件。页面此时只展示维度含义、权重及可选的期限/预算/资源约束，不预填候选或虚构评分。页面HTML生成在会话工作区内，生成后立即用文件交付工具声明为交付物，供用户在平台预览面板打开；确认后直接接续任务，其他环境使用原生输入或对话接收偏好，不要求用户操作JSON或命令。

网页预览环境（如浏览器版宿主、沙箱iframe预览）无法自动打开外部浏览器，也不保证剪贴板可用：**这类环境优先用对话或原生输入工具接收偏好**，页面仅作为可查看的参考，不依赖页面回传。本地回环接收器只在用户本机桌面、且回环地址对用户可达时才使用；云端、远程和网页宿主都不把服务器地址当作用户可访问的地址。

用户已明确给出权重、接受初始配置或授权技能自行设置时直接记录并沿用；只做审计、单一既定方案、实验或写作时不强加此步骤。预填权重、浏览或调整页面不等于提交；等待期间可进行与偏好无关的数据核验，收到确认前不开始候选生成。续作复用同版本偏好，不重复弹窗。

权重影响机会搜索的侧重和后续比较，不改变事实、评分锚点或证据要求；硬约束单独记录，不由高分抵消。保留有依据且实质不同的候选，不为迎合偏好倒推分数或编造机会。后续比较页沿用已确认权重，用户看到具体方案后仍可调权，最终方案另外由用户选择。

## 1．先审核数据，再建立证据底座

**首次使用某数据源提出想法前，先完成与本轮用途相关的数据审核。** 按 [数据核验](references/data-audit.md) 实际读取说明文件与数据，将文档声称的结构、字段语义、单位、时间、标签和处理来源逐项对照实测，记录一致、冲突、未知与不适用；没有说明文件时如实记录，不能自行补写字段含义。说明与数据相符仅证明已核验范围内的一致性，不能据此认证现实测量准确。

机会发现先核验支撑候选的必要结构、语义和具体事实；入选后再深化相关标签、时点、独立性和评价检查。记录实际读取范围，样本检查不能冒充全量核验。版本和用途未变且已有对应证据时复用审核；新增文件、版本或会改变结论的处理触发受影响部分复核，不另做重复的全库审核。

核心字段、实体身份、时间口径或标签含义存在未解决冲突时，暂停依赖它们的选题判断、实验和价值测算，先定位来源、文档或解析问题；不影响其他已核验数据的使用。可以提出数据质量核验或修复任务，但不能把可疑现象当作已证实的研究发现或业务机会。任何修正保留原始材料、依据和前后差异，并重新核验。

学术任务在利用结果关系筛选假设前，按 [学术细则](references/academic-writing.md#探索与确认边界) 明确探索资料及后续确认方式。基础结构核验与影响选题的分析分别记录；看过结果再切分，不能自动恢复独立确认资格。

将材料描述与实测分开；保留冲突和历史处理身份。核心结论标记证据来源与完成状态，至少能定位到数据字段、文档位置、查询结果或实验输出。提供数据可承担的角色：主分析、开发、独立验证、机制对照、敏感性或仅案例；不强行把所有数据拼成训练集。

**本阶段出口：** 给出说明与实物对照结果、未解决问题及其影响，并明确哪些字段或数据在已核验范围内可用、须限定用途，或暂不能支撑相关判断；可并入现有证据表。说明每个数据集实际观察了什么、没有观察到什么及当前可支持的任务。尚未成立的字段语义、真值或独立性不得在后续悄悄变成已知。

## 2．从事实提出问题

先识别数据提供的观察能力，再结合领域知识连接研究问题、业务动作或服务，按 [问题、验证与交付](references/problem-validation.md) 的启发路径形成少量候选。说明为什么这份数据使该问题值得提出，以及什么观察可能推翻它；数量由数据与任务决定，不为凑数编想法。每个候选连接：

1. 具体事实与字段；据此发现的现象、尚未利用的信息或现有做法不足之处。
2. 谁需要回答什么问题；答案将改变什么科学认识、业务动作，或使哪种新服务成为可能。
3. 输入何时可用、输出是什么、标签或结果如何得到。
4. 最简单可行办法；新增方法相对它改变了什么。
5. 最便宜的反证或试验、成本、成功与停止条件、失败后的有效交付。

候选应在研究问题、解释机制、业务动作或服务对象上存在实质差异；仅更换模型、损失函数或技术名称通常属于同一候选的实现变体。没有理由的模型叠加不是想法；数据数量多不是独立样本多；缺少某字段是本项目约束，不自动等于全领域研究空白。

标明候选是**机会假设、可验证方案，还是已获支持方案**，具体门槛见上述细则。机会假设需有事实线索，可保留明确未知的标签、对照或行动条件，并给最低成本补证据路径；不能将其写成已可实施或已有效。区分暂时不能验证、值得补证据与已证实不成立，不按成熟方案标准提前淘汰所有早期机会。

## 3．查清已有工作与价值边界

先消化用户的数据、已有项目和代码，再按具体问题检索公开研究或现成解决办法。核实标题、作者、时间、出处、稳定链接及实际阅读程度。搜不到不等于不存在；只读摘要不能断言对方没有某项实验。

- 学术：读取 [学术研究与写作](references/academic-writing.md)。比较领域工作、方法近邻、团队已有成果；贡献可来自问题、测量、识别设计、数据证据或算法，但须说清新增量。
- 企业：读取 [企业价值落地](references/business-value.md)。先从数据发现可新增的用途、业务动作或服务，再比较现有流程、人工规则、SQL/报表、采购工具和模型；选择实现机会所需的最低合理复杂度。论文新颖性不是企业方案的必需门槛。

## 4．先审查，再扩大投入

按本轮范围和候选成熟度切换到审查视角；同一执行者的自查不称为独立审稿。按影响排序，区分：已证实错误、需实验的风险、现有条件无法支持的主张。每项都给证据、影响范围、修正/降级及可判断的验收标准。

按实际主张选择适用的检查，不将尚未进入的阶段列为未通过：

- 对象一致：观测值、代理标签、参数估计、预测分布和因果反事实不互换。
- 规则可用：是否存在合理输入能产生有效输出；全拒判、全不告警或全不行动是否被指标错误奖励。
- 时点正确：涉及未来、在线、纵向或重复实体的任务，按信息可用时刻及独立单元切分。随机切分仅在目标确实允许、且能说明其对应泛化对象时采用。
- 比较公平：同信息、同预测提前量、同标签、同支持集及相称的调参/反馈预算；架构可复用不等于权重或节点身份可直接迁移。
- 分母正确：缺测、拒判、未发生、持续未完成和删失不能混为一类；种子、滑窗和合成情景不增加独立事件或客户数。
- 主张适度：相关解释不当因果；可运行不当有效；统计改善不当业务改善；未知预处理不当已认证在线数据。

已证实的硬伤先处理，不能用加权高分抵消；关键条件未知时，先判断补证据的价值与可行性，不直接等同于无效。评分用于透明比较，不输出伪精确录用概率或收益保证。避免反复换更复杂模型拖延停止决定。

### 评分、可视化与用户选择

本轮包含多方案筛选时，按 [候选评分与用户选择](references/candidate-scoring.md) 先去重与审查，再默认精选三个有依据、实质不同的方案作详细比较；不足三个如实展示，不为凑数新增方案。用户已指定单一方向、只做审计或写作时，不强行增加候选。

学术版比较创新度、研究意义、数据适配、实施可行性和验证设计；企业版比较潜在净价值、见效时间、执行成本、落地条件和效果可测量性。沿用生成候选前已确认的权重，使用有锚点的1–5分，逐项附依据；未知不填中间分，硬伤不由总分抵消。说明本次评分用于优先验证还是优先实施，成熟度与评分分别展示。

给出评分矩阵及统一尺度的条形图，并附各方案优势、关键风险、时间/成本估算和下一步。**到达比较步骤时，由技能准备评分数据、生成可调权重的交互页面并主动展示**：把HTML生成在会话工作区内，生成后立即用文件交付工具声明为交付物，让用户可以一键在平台侧边栏或预览面板打开；平台可预览交互HTML时优先用原生预览，在能打开用户浏览器的本地桌面环境才调用随附脚本的 `--open`。展示不等于用户已查看：本地打开请求失败或被拒绝时保留可点击文件，不反复弹窗。具体展示与降级方式见评分细则，不要求用户复制JSON、运行命令或另装评分工具。工具受限时直接提供可点击报告或表格与文字条形表示。图表使用同一份评分数据，不把未知画成零分，不把主观总分写成成功概率。

**展示完成后由用户选择方案。** 可以给建议并解释取舍，但不得自动把最高分设为主线。使用可用的输入工具或简短文字，请用户选择具体方案，也允许先补证据、调整权重或暂不选择；收到明确选择前，不启动选定方案的后续实验、开发或业务试点。未回复、界面预选或仅浏览图表不算选择。已有明确选择且方案范围未变时直接沿用；选择只确定方向，不扩大执行权限。

## 5．做能改变判断的最小验证

本轮包括验证时，按任务预算选择最小、可逆的范围，先记录待验证问题、判断办法、预算和去留门槛。早期机会可先核验关键字段语义、标签获取或行动可行性；需要评估效果时，再明确主要指标、简单基线和评价范围。先查相关单位、掩码、连接倍增、评价器和退化规则，再归因于模型。数值门槛来自决策代价、精度要求或明确的探索设定，不照搬历史项目。

建模时优先建立强简单基线，再定位残差或失败机制；必要时增加第二阶段组件。两阶段是待验证的工程选择，不是禁止端到端方法的定律。纯数据处理项目不强制训练模型。

运行已授权的必要验证，保存配置、版本、预测/决策时点与输出；没运行就标为计划。只增加能检验具体风险的负控和测试。开发中改变的假设与阈值要留痕，不能追认成事先规定。

**本阶段出口：** 继续、修正、收缩或停止当前路径，附证据与下一步。失败仍可交付数据质量修复、边界结论或成本比较，但不能将普通失败自动包装成论文贡献。

## 6．落实为实现与写作

本轮要求工程交付时，依据 [交付契约](references/problem-validation.md#工程交付) 输出工程师可以独立执行的任务：输入字段、处理规则或张量/损失、输出、依赖、验收、资源估算与失败处理。没有机器学习的流程用表、键、查询和规则表达，不硬塞编码器。

本轮要求写作时，按学术或企业流程组织文稿；每个重要主张回连证据与实验状态。最终文稿必须区分“提出”“实现”“验证”“部署”和“已产生收益”。删去已淘汰方向的优越性表述，并同步已有的摘要、图表、指标和开发任务。

面向用户展示精选证据、判断和决定；完整日志留在文件中。交付大小与用户需求相称，不把每次咨询变成长篇报告或固定数量的文件。

## 按需材料

- [数据核验](references/data-audit.md)：首次使用前对照说明与实物；新增文件、版本、跨文件合并，或时间/标签/来源存疑时复核相关部分。
- [问题、验证与交付](references/problem-validation.md)：生成候选、筛选、设计最小实验、写开发任务时。
- [候选评分与用户选择](references/candidate-scoring.md)：生成想法前设置偏好，以及精选三个方案、评分可视化并等待用户决定时。
- [学术研究与写作](references/academic-writing.md)：选题、查新、实验到文稿、审稿答复时。
- [企业价值落地](references/business-value.md)：从未充分利用的数据发现机会、开发业务想法、设计试点、检验价值与实施交接时。
- [历程提炼的判断案例](references/decision-lessons.md)：对象含混、全拒判、小样本、迁移或代理价值等问题出现时；案例数字仅解释判断，不作为新项目参数。

## 结束前检查

仅核对本轮交付适用的项目：版本由使用者明确选择；支撑主张的数据实际读取范围清楚；冲突和处理身份保留；候选有明确对象、价值和成熟度，关键未知有补证据路径；企业想法能追溯到已核验数据且未把预期收益写成已实现；最近方法/现有流程比较支持本轮判断；学术选题的探索与确认身份清楚；标签与信息可用性一致；评价不奖励无效输出；小样本与独立性如实报告；简单办法参与比较；成本和停止条件明确；本轮需要的开发任务可验收；结论与已完成结果一致。

检查状态区分“已完成”“影响本轮结论但尚未完成”和“不适用”；对尚未完成项说明影响，不以“核对通过”掩盖缺口，也不把未进入的工程、实验或部署阶段算作本轮缺陷。

涉及多方案比较时，确认生成前的偏好已有用户确认或明确授权，评分依据、权重及图表一致，未知没有被填成分数，已分别记录偏好确认与方案选择的状态；不能将最高分、界面预选或未回复记为用户决定。
